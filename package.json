{
  "name": "dropship-video-engine",
  "version": "0.1.0",
  "description": "Motor local de producción de video para dropshipping (pipeline asíncrono con cola y reintentos).",
  "type": "module",
  "private": true,
  "engines": { "node": ">=20" },
  "bin": { "engine": "bin/engine.js" },
  "scripts": {
    "test": "node --test tests/*.test.js",
    "engine": "node bin/engine.js"
  }
}
